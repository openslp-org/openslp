# Automatically added by dh_installdocs
if [ "$1" = "configure" ]; then
	if [ -d /usr/doc -a ! -e /usr/doc/libslp0 -a -d /usr/share/doc/libslp0 ]; then
		ln -sf ../share/doc/libslp0 /usr/doc/libslp0
	fi
fi
# End automatically added section
